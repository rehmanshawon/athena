{
  "name": "athena",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:api": "npm --prefix apps/athena-api run dev",
    "dev:web": "npm --prefix apps/athena-web run dev",
    "build:api": "npm --prefix apps/athena-api run build",
    "build:web": "npm --prefix apps/athena-web run build"
  }
}
